# WUTA-FSD 节点与话题速查表

## 感知 (Perception)

| 节点 | 输入话题 | 输出话题 |
|------|------|------|
| `lidar_detection_node` | `/hesai/pandar` (PointCloud2) | `/perception/lidar/cones` (ConeArray) |

## 定位 (Localization)

| 节点 | 输入话题 | 输出话题 |
|------|------|------|
| `kiss_icp_node` | `/hesai/pandar` (PointCloud2) | `/kiss/odometry` (Odometry) |
| `ekf_node` | `/kiss/odometry` (Odometry) | `/odometry/filtered` (Odometry) |
| | `/cg410/odometry` (Odometry) | |
| `localization_manager` | `/odometry/filtered` (Odometry) | `/localization/pose` (PoseStamped) |
| | `/ndt/pose` (PoseStamped) | `/system/localization_ready` (Bool) |
| | `/system/mission_state` (MissionState) | |
| `ndt_localization_node` (RACE,单独启动) | `/hesai/pandar` (PointCloud2) | `/ndt/pose` (PoseStamped) |
| | `/initialpose` (PoseWithCovarianceStamped) | `/ndt/path` (Path) |
| | `/system/mission_state` (MissionState) | `/ndt/aligned_cloud` (PointCloud2) |

## 建图 (Mapping)

| 节点 | 输入话题 | 输出话题 |
|------|------|------|
| `cone_map_builder_node` | `/perception/lidar/cones` (ConeArray) | `/mapping/cone_map` (ConeMap) |
| | `/localization/pose` (PoseStamped) | `/mapping/cone_map_viz` (MarkerArray) |

## 规划 (Planning)

| 节点 | 输入话题 | 输出话题 |
|------|------|------|
| `boundary_detector_node` | `/mapping/cone_map` (ConeMap) | `/planning/centerline` (Lane) |
| | `/localization/pose` (PoseStamped) | `/planning/centerline_viz` (MarkerArray) |
| | `/system/mission_state` (MissionState) | |
| `path_generator_node` | `/planning/centerline` (Lane) | `/planning/final_waypoints` (Lane) |
| | `/localization/pose` (PoseStamped) | |
| | `/system/mission_state` (MissionState) | |

## 控制 (Control)

| 节点 | 输入话题 | 输出话题 |
|------|------|------|
| `controller_node` | `/planning/final_waypoints` (Lane) | `/control/command` (Command) |
| | `/localization/pose` (PoseStamped) | `/control/target_viz` (MarkerArray) |
| | `/localization/velocity` (TwistStamped) | |
| | `/system/mission_state` (MissionState) | |

## 数据流

```
/hesai/pandar ──→ lidar_detection ──→ /perception/lidar/cones ──→ cone_map_builder ──→ /mapping/cone_map
       │                                        ↑                           │
       ├──→ kiss_icp ──→ /kiss/odometry ──→ ekf ──→ /odometry/filtered     │
       │                                                    │               │
       │                                          localization_manager      │
       │                                                    │               │
       │                                          /localization/pose ───────┘
       │                                                    │
       └──→ ndt_localization ──→ /ndt/pose ─────────────────┘
                                                             │
                          /mapping/cone_map ──→ boundary_detector ──→ /planning/centerline
                                                                           │
                                                  /planning/centerline ──→ path_generator ──→ /planning/final_waypoints
                                                                                                      │
                                                              /planning/final_waypoints ──→ controller ──→ /control/command
                                                                      ↑
                                                            /localization/pose
                                                            第一阶段（修配置）→ 第二阶段（补 launch）→ 第三阶段（联合启动）                          │
     │                                                     │                                    │
     │                                     第四阶段（无传感器跑通全链路）                       │
     │                                                     │                                    │
     │                                     第五阶段（mission_manager 补全）                     │
     │                                                     │                                    │
     │                                     第六阶段（camera + fusion）
     
     
     
      系统中所有订阅话题中，来自外部传感器/硬件的有：

  ---
  必需传感器
 
  传感器: Hesai 128 LiDAR
  话题: /hesai/pandar
  消息类型: PointCloud2
  消费者: kiss_icp_node、lidar_detection_node、ndt_localization_node
  说明: 三个节点共用一个话题
  ────────────────────────────────────────
  传感器: CG-410 INS
  话题: /cg410/odometry
  消息类型: Odometry
  消费者: ekf_node
  说明: GNSS RTK + IMU 融合位姿，校正 KISS-ICP 漂移
  ────────────────────────────────────────
  传感器: 车速（CAN）
  话题: /localization/velocity  
  消息类型: TwistStamped
  消费者: controller_node
  说明: 车辆当前速度，Pure Pursuit 计算前视距离用

  ---
  人机交互输入
 
  ┌─────────────────────────┬────────────────────────┬─────────────────────┬────────────────┐
  │          话题           │        消息类型        │       消费者        │      说明      │
  ├─────────────────────────┼────────────────────────┼─────────────────────┼────────────────┤
  │                         │ PoseWithCovarianceStam │ ndt_localization_no │ NDT            │
  │ /initialpose            │ ped                    │ de                  │ 初始位姿（RACE │
  │                         │                        │                     │  模式用）      │ 
  ├─────────────────────────┼────────────────────────┼─────────────────────┼────────────────┤
  │ /system/emergency       │ Bool                   │ mission_manager     │ 急停按钮       │
  ├─────────────────────────┼────────────────────────┼─────────────────────┼────────────────┤
  │ /system/mission_mode_cm │ String                 │ mission_manager     │ 赛道模式切换命 │
  │ d                       │                        │                     │ 令             │
  ├─────────────────────────┼────────────────────────┼─────────────────────┼────────────────┤
  │ /system/lidar_ready     │ Bool                   │ mission_manager     │ LiDAR          │
  │                         │                        │                     │ 驱动就绪信号   │
  ├─────────────────────────┼────────────────────────┼─────────────────────┼────────────────┤
  │ /system/inspection_trig │ Bool                   │ mission_manager     │ 触发传感器自检 │
  │ ger                     │                        │                     │                │
  └─────────────────────────┴────────────────────────┴─────────────────────┴────────────────┘

  ---
  将来源加（camera_detection 空壳）
  
  ┌───────────────┬───────────────────────────────────┐
  │    传感器     │               用途                │
  一句话

  当前需要 3 个外部传感器驱动节点：Hesai LiDAR 驱动、CG-410 INS 驱动、CAN/VCU 车速上报。这 3
  个不在本仓库中，是独立的硬件驱动包。

```
